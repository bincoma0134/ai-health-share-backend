**Lưu ý quan trọng:** Hãy coi toàn bộ các tài liệu tôi cung cấp (AI_RULES.md, Context, cấu trúc dự án, source code, tài liệu thiết kế, yêu cầu trước đó,...) là **nguồn sự thật (Source of Truth)** và áp dụng xuyên suốt cho mọi phân tích, nhận định, đề xuất và chỉnh sửa. Không được bỏ qua, không được mâu thuẫn với các tài liệu này, không được suy đoán khi chưa đủ dữ liệu; nếu thiếu thông tin hoặc file cần thiết, hãy yêu cầu tôi cung cấp trước khi tiếp tục.
- Xác nhận lại toàn bộ thông tin, sau đó lấy ví dụ để chứng minh cậu đã hiểu đúng nguyên tắc Ctrl + H.


------------

Triển khai Tầng 1.1 . Chiến lược vẫn như vậy, nâng cấp chỉnh sửa thì sử dụng nguyên tắc, tuân thủ AI_RULES.md, Ctrl + H. Tạo mới thì chỉ rõ tên file và đường dẫn. Từ bước 1 đến hết phase liền mạch. Không cần giải thích, chia phần dài dòng. Có nghiệm thu cuối cùng rõ ràng, đảm bảo tăng tối đa xác suất tôi hiểu đúng, do đã không có giải thích, nên nghiệm thu phải đặc biệt chặt chẽ.
-------------

1. Tổng quan theo luồng (Chia phase lần 1 - theo luồng);
2. Tổng hợp các thay đổi ở database và backend (bảng nào cần tạo mới, file nào cần tạo mới, file nào cần chỉnh sửa và cụ thể cần chỉnh sữa những gì, phục vụ cho luồng nào) (Chia phase lần 2 - theo tầng, bắt đầu triển khai theo phase tại đây);
3. Frontend;
#Nghiệm thu chặt chẽ (test) sau mỗi lần sửa, nâng cấp code. 


*Được, hãy triển khai sửa code, cô lập, xác định, giải quyết gọn, không mở rộng ảnh hưởng đến các logic khác. Có **nghiệm thu** đầy đủ. Tuân thủ AI_RULES.md và nguyên tắc Ctrl + H. Khi triển khai code, dự đoán các trường hợp phổ biến lỗi có thể xảy ra, chèn các đoạn - print debug lỗi.

Chưa cần code, audit, đề xuất giải quyết gọn, không mở rộng ảnh hưởng đến các logic khác. Xác định vấn đề thật sự, không ngộ nhận, ảo giác. Lấy dẫn chứng chứng minh.


npm run build
npx wrangler pages deploy dist --project-name=miniworld-shop

npm run build
npx wrangler pages deploy dist --project-name=miniworld-admin

npx wrangler deploy

npx wrangler tail
----------

cd /d E:\AI Project\AI Project_AI Health Share\frontend_mobile

E:\src\flutter\bin\flutter analyze

taskkill /F /IM java.exe

taskkill /F /IM dart.exe

rmdir /s /q build
----------------
E:\src\flutter\bin\flutter clean

E:\src\flutter\bin\flutter pub get

%LOCALAPPDATA%\Android\Sdk\platform-tools\adb reverse tcp:8000 tcp:8000

E:\src\flutter\bin\flutter devices

E:\src\flutter\bin\flutter run -d R58N30APMHD

E:\src\flutter\bin\flutter run --release -d R58N30APMHD



E:\src\flutter\bin\dart run flutter_launcher_icons

E:\src\flutter\bin\flutter create . --platforms=ios
----------------
%LOCALAPPDATA%\Android\Sdk\platform-tools\adb logcat -c

%LOCALAPPDATA%\Android\Sdk\platform-tools\adb logcat AndroidRuntime:E ActivityManager:E flutter:V *:S

---------
%LOCALAPPDATA%\Android\Sdk\platform-tools\adb reverse tcp:8000 tcp:8000

E:\src\flutter\bin\flutter attach -d R58N30APMHD

E:\src\flutter\bin\flutter doctor -v

./.venv/scripts/activate
uvicorn main:app --host 127.0.0.1 --port 8000 --reload


--------------
# 1. Di chuyển vào thư mục dự án Backend và kiểm tra trạng thái thay đổi

git status

git add .

git commit -m "BIG UPDATE BETA 02_Phase 06"

git push origin main


